 function isTasksCompleted(capID, wfstr) //, stask){    // returns true if any of the subtasks are active    var taskResult = aa.workflow.getTasks(capID);    if (taskResult.getSuccess())    { taskArr = taskResult.getOutput(); }    else    { aa.print("**ERROR: getting tasks : " + taskResult.getErrorMessage()); return false }    for (xx in taskArr){        if (taskArr[xx].getTaskDescription().toUpperCase().equals(wfstr.toUpperCase())) {            aa.print("Is Task Active:" + taskArr[xx].getActiveFlag());            aa.print("Is getCompleteFlag Active:" + taskArr[xx].getCompleteFlag());            if (taskArr[xx].getActiveFlag().equals("Y")) {                return true;            } else {                return false;            }        }    }}//used by others dont remove